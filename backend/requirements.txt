# SYVREN backend — ultra-minimal for Vercel Hobby (250 MB limit).
# Vercel's @vercel/python provides its OWN ASGI server; we do NOT need uvicorn in the bundle.
fastapi==0.110.1
motor==3.5.0
pydantic>=2.6.4
python-dotenv>=1.0.1
pypdf==4.3.1
httpx>=0.27.0
